[package]
name = "research-companion-native"
version = "0.2.0"
description = "Tauri desktop client for Research Companion OS"
authors = ["Research Companion OS"]
edition = "2021"

[lib]
name = "research_companion_native_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
rfd = "0.15"
